# 0.1.4 validation — 2026-09-13

- Configuration restoration tests: enabled, disabled, absent key, explicit null, invalid types, invalid JSON, and missing file; settings remain unchanged.
- LaunchAgent plist test: explicit user LaunchAgents and Logs directories, without starting a service.
- Both XCTest cases passed using the installed Xcode 27 toolchain.
- Homebrew 7.0.0: cask style checks passed; the structured postflight artifact loaded successfully. A harmless probe executed through Homebrew's actual postflight sandbox verified account-home resolution, existing-config read access, and read-only access to the user's launchd domain. No installed service was restarted for these tests.
- Release build: Apple Swift 6.4 (`swiftlang-6.4.0.34.1`), Command Line Tools macOS 27 SDK, arm64. Both executables report minimum macOS 13.0 and SDK 27.0.
- The current Command Line Tools `swiftbuild` backend emitted SDK 13.0 in Mach-O metadata despite using the macOS 27 SDK. This release used `SWIFT_BUILD_SYSTEM=native`; the package script now accepts an optional backend override and passes the selected macOS SDK explicitly.
- Both executable and app signatures verified; package signature verified; CLI help smoke test passed.
- Apple notarization accepted: `9b43c897-eea1-4a66-b5c1-bf209dd9f71c`; ticket stapled and validated.
- Final package SHA-256: `12feb15357ea915ae951afc1411c1bd58c36f667bcb63a23c54b7091391280d8`.

The final package is `VirtConnector-0.1.4-signed.pkg`. This validation does not perform a live install or upgrade on the developer's machine.
