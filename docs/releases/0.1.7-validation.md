# 0.1.7 validation — 2026-09-21

## Status

Signed GUI package built and independently verified. **Notarization and public release are pending**: neither the previously documented `virt-connector-notary` nor `git-labeler-notary` Keychain profile was available to notarytool. A dedicated registration helper and fixed release workflow were subsequently added; a fresh build using that workflow is required after the user registers the new password. No credentials were created or changed by the agent. The Cask remains on 0.1.6 until the new package is notarized, stapled, and its final checksum is known.

## Checks completed

- `swift test --build-system native --quiet`: **58 passed, zero failures**. The default-backend suite also passed during GUI verification.
- `python3 -B -m unittest scripts/test-postinstall.py scripts/test-packaging.py`: **21 passed**.
- After fixing the release procedure, `python3 -B -m unittest scripts/test-release.py scripts/test-packaging.py scripts/test-postinstall.py`: **32 passed**. Covers missing credentials stopping before builds, fixed profile/keychain selection, published-version protection, rejected notarization, post-staple checksums, and package trust/content verification with mocked external tools.
- Shared native presentation preview `--check`: passed for Japanese and English, including default-button safety, Escape, state gating and recovery.
- GUI rendering and keyboard evidence: see the [GUI review](../reviews/2026-09-21-apple-hig-gui.md).
- Shell syntax and `git diff --check`: passed.
- Homebrew style for the Cask and HEAD-only Formula: two files, no offenses.
- Clean isolated production build with Xcode 27.0 (27A266a), macOS SDK 27.0.
- Package version `0.1.7`, identifier `st.rio.virt-connector.pkg`, install location `/`.
- Extracted CLI and daemon are arm64 with minimum macOS 13.0 and SDK 27.0. App Info.plist version and minimum OS match.
- Strict CLI and app signature verification passed; pkgutil confirms a trusted timestamp and Developer ID Installer signature.
- Extracted installer scripts and packaged README match source byte-for-byte. Executable and installer-script permissions are 0755.
- Signed CLI `--help` smoke test passed without device or service actions.

pkgbuild emitted `write: Permission denied` diagnostics but exited successfully; the independent package-content and signature checks above passed. The same diagnostic was recorded for 0.1.6.

## Remaining release gates and limits

- Apple notarization acceptance, stapling, Gatekeeper assessment, final checksum, Cask/tap update and public GitHub release remain pending.
- The GitHub 0.1.7 release is a draft with the earlier signed, unnotarized GUI package. Replace it with the fresh verified artifact and update its target commit before publication; follow [RELEASING.md](RELEASING.md).
- No privileged install/upgrade, real system shutdown/cancellation, or physical device-state test was performed. The installed agent was left running.
- macOS 13 runtime and VoiceOver task execution remain untested. Distribution remains Apple Silicon-only.
