# 0.1.5 validation — 2026-09-15

The reported upgrade downloaded and installed 0.1.4, then failed during `launchctl bootstrap` inside Homebrew's postflight sandbox. Running the same installed `restore-agent` outside that sandbox succeeded and restored the user's stopped LaunchAgent.

Validation completed:

- Three installer-script regression tests cover console-user privilege selection, no-user/alternate-volume skipping, and nonfatal restoration failures with retry instructions.
- The exact restoration command used by postinstall (`launchctl asuser 501 sudo -n -H -u rifujita ... restore-agent`) succeeded in the real user session. `launchctl print` confirmed the service was running.
- The signed package's extracted postinstall script matches the tested source.
- Build: Apple Swift 6.4, native SwiftPM backend, macOS 27 SDK, arm64, minimum macOS 13.
- Homebrew cask style check passed. The cask contains no flight-step hook.
- Apple notarization accepted: `c1f61245-bf93-456d-89fa-20b264c8a327`; ticket stapled and validated; Gatekeeper accepted the package.
- Package SHA-256: `119a3fda65fff95b8bcfbf6a0ea58f7b42de10c8b7deb71219519d64ad07ebd2`.

No full privileged package upgrade was performed during pre-release validation: this session has no cached administrator authorization. Installer script behavior and actual user-session restoration were checked separately.
